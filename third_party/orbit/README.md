# Orbit Helper

`Orbit.h` and `Orbit.cpp` are copied from the ANARI-SDK viewer support code and
retain their original Apache-2.0 license header.

The course treats this as third-party application infrastructure. The ANARI
lesson is how the helper's `eye()`, `dir()`, and `up()` values are connected to
ANARI camera parameters.
