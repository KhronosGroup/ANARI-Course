# tinygltf v3

`tiny_gltf_v3.h`, `tiny_gltf_v3.c`, and `tinygltf_json_c.h` are copied from
the tinygltf v3 source tree and retain their original MIT license.

The course uses this as a small, vendored glTF parser for the glTF loader
checkpoint.
