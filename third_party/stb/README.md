# stb_image_write

`stb_image_write.h` is vendored from the public-domain stb single-header image
writing library.

Each PNG-writing checkpoint defines `STB_IMAGE_WRITE_IMPLEMENTATION` locally so
the checkpoint remains self-contained.
